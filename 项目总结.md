# 项目总结 · 智能推荐系统全链路 + 后训练

> 面试使用请优先阅读 `docs/INTERVIEW_PLAYBOOK.md`、`docs/RESUME_PROJECT.md`
> 和 `docs/PROJECT_EVIDENCE.md`。本页保留技术全景，不建议直接整页复制进简历。

> 一句话：**独立实现工业级推荐系统全链路（召回→排序→在线策略），并对齐大厂 LLM
> 的"后训练"范式（SFT→RL），完成推荐版的 SFT + 离线强化学习。** 全程可复现、可部署、可测试。

---

## 一、系统架构（数据 → 模型 → 服务 → 后训练）

```
MovieLens-1M/25M 日志
      │  预处理 / leave-one-out / 负采样
      ▼
┌───────────────── 离线训练 ─────────────────┐      ┌──────── 在线服务 ────────┐
│  双塔召回(Faiss) ── DeepFM 精排           │      │  serving.Recommender     │
│  SASRec 序列建模(用户模型)                 │─权重→│  app.py(Streamlit Demo)  │
│  Bandit / DQN / 策略梯度(会话级 RL)        │      │  serve.py(REST API ~5ms) │
└────────────────────────┬───────────────────┘      └──────────────────────────┘
                         │  冻结 SASRec 为用户模型，构建含疲劳/厌倦的会话 MDP
                         ▼
          ┌──────── 后训练(Post-Training) ────────┐
          │  SFT：行为克隆 + 奖励加权 BC           │  ← 对应 LLM 的 SFT
          │  离线 RL：CQL / naive 离线 DQN         │  ← 对应 LLM 的 RLHF
          └───────────────────────────────────────┘
```

## 二、关键指标一览

| 环节 | 模型 / 策略 | 指标 | 结果 |
|------|-------------|------|------|
| 召回基线 | 矩阵分解（手写 SGD）| Recall@10 | 0.062 |
| CTR 排序 | LR / FM / **DeepFM** | 测试 AUC | 0.707 / 0.752 / **0.754**（时间切分）|
| 召回 | 双塔 + logit 校正 | Recall@10 | **0.097**（vs MF +56%）|
| 在线探索 | LinUCB vs ε-greedy | 在线 CTR | **0.704** vs 0.671 |
| 序列推荐 | SASRec 复现 | HR@10 / NDCG@10 | **0.791 / 0.543** |
| 会话 RL | 策略梯度（采样部署）| 会话奖励 | **15.86**（朴素贪心 2.81 的 5.6×；贪心去重基线 17.93）|
| **后训练** | SFT（行为克隆）| 会话奖励 | 2.87（= 行为策略天花板）|
| **后训练** | 离线 RL（naive DQN）| 会话奖励 | **8.03 ~ 13.59**（越过 SFT，学出交错策略）|
| 服务 | 双塔→DeepFM REST | 单请求延迟 | **~5 ms**（模型常驻）|

> 见 `figures/13-离线RL后训练对比.png`、`figures/14-CQL保守系数权衡.png`。
> 指标为各自评估协议下的结果，RL 部分为仿真器证据（详见各 notes 与 `research/研究报告.md`）。

## 三、讲给面试官的三条故事线

1. **两段式架构为什么这么设计**：召回要"快"（Faiss 内积全库毫秒级，3706→50），
   排序要"准"（DeepFM 只重排 50 个候选）。若让 DeepFM 直接打全库分，延迟约放大 74×——
   这就是工业界召回→排序分层的根本原因，我用 `serve.py` 的实测延迟量化了它。

2. **推荐系统的"后训练"与 LLM 同构**：SFT = 行为克隆（模仿日志，**天花板就是行为策略**，
   实测 BC 保真度 99.7% → 恰好复刻 2.87）；RL = 离线 Bellman 提升（越过天花板到 8~13.6，
   学出"交错类型抗疲劳"的策略）。奖励加权 BC 就是 LLM 的"拒绝采样 SFT / best-of-N"。

3. **离线 RL 落地的第一课是"先诊断，再选药"**：教科书说 CQL>naive，我实测在本仿真器里
   **naive 反而更强、CQL 的保守单调减分**（α 权衡曲线）。诚实归因：回报有界（厌倦退出+LayerNorm）
   + 动作全合法 → 驱动 CQL 的"外推误差灾难"不成立。**知道何时该保守，比会调一个 SOTA 更重要。**

## 四、可直接抄进简历的 bullet（中文）

- **推荐系统全链路**：独立实现工业级"召回→排序"两段式推荐系统——双塔模型 + Faiss 向量检索
  毫秒级召回，DeepFM 精排（测试 **AUC 0.754**，Recall@10 **0.097**，较 MF 基线 **+56%**）；
  封装为 Starlette REST 服务，单请求端到端 **~5 ms**，含产物自检与优雅降级。
- **序列推荐 + 强化学习**：复现 SASRec 自注意力序列推荐（**HR@10 0.79 / NDCG 0.54**）；
  构建含疲劳/厌倦动力学的推荐会话 MDP 仿真环境，手写 Double DQN 定位并修复训练发散的"致命三要素"，
  策略梯度采样部署会话奖励达朴素贪心基线的 **5.6×**（贪心去重基线 17.93）。
- **推荐系统后训练（对标 LLM SFT→RLHF）**：实现离线后训练流水线——行为克隆（SFT）+ 奖励加权 BC
  + CQL 离线强化学习；量化 **SFT 天花板（2.87）→ 离线 RL 越过（9.3~13.6）**，
  并给出 CQL 保守性适用边界的实验分析（数据覆盖 / 回报结构诊断）。
- **Agentic 推荐工具门控**：参考 DeepEyes 的主动感知思想，实现“直接推荐 / 疲劳感知重排”
  两动作智能体、显式工具成本、条件奖励和 group-relative REINFORCE；300 个同种子仿真会话中，
  规则门控在保持用户奖励 **17.947** 的同时减少 **12.8%** 工具调用，且实验性 RL 退化后明确不予上线。
- **工程化**：标准库 `unittest` 当前发现 **73 用例**（71 通过、2 个真实 checkpoint
  集成测试按配置跳过；CPU-only、无网络依赖，一键 harness 触发）；
  统一随机种子 + 配置快照实现全实验可复现；Streamlit Demo + REST API 双入口同源。

**技术栈**：Python · PyTorch · Faiss · pandas/NumPy · Starlette/uvicorn · Streamlit · unittest

## 五、一键复现

```bash
.venv/Scripts/python.exe scripts/ai_startup_harness.py --check   # 环境/产物自检
.venv/Scripts/python.exe scripts/ai_startup_harness.py --test    # 冒烟测试(73 用例)
.venv/Scripts/python.exe -m streamlit run app.py                 # 交互 Demo
.venv/Scripts/python.exe serve.py --port 8000 --preload          # REST API
.venv/Scripts/python.exe research_v2/phase2_offline_rl/train.py --smoke   # 后训练全流程
.venv/Scripts/python.exe src/plot_offline_rl.py                  # 生成后训练图表
```

**延伸阅读**：`notes/12-推荐系统后训练-离线RL与SFT.md`（原理+推理+诚实结论）、
`notes/11-工程化落地-测试与服务化.md`、`research/研究报告.md`、`REPORT.md`。
