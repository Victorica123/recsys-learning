# 智能推荐系统实战 · 项目总报告

> 作者：Victorica123 ｜ 完成日期：2026-07-18
> 硬件：RTX 5060 Ti + Ryzen 5 9600X + 32GB ｜ 环境：Python 3.12 + PyTorch 2.9.1+cu128

## 摘要

基于 MovieLens-1M（6040 用户 / 3706 电影 / 100 万评分），完整复现了
互联网推荐系统的核心技术栈：**数据探索 → 矩阵分解 → 显式偏好深度排序
（复用 LR/FM/DeepFM 等 CTR-style 架构，但标签不是点击）→ 双塔召回 + Faiss
向量检索 → Contextual Bandit 探索-利用**，
并交付一个可交互的推荐 Demo。端到端评估证明旧 DeepFM 精排为净损失后，
当前 Demo/API 默认直接服务双塔 Top-K，精排只保留为显式实验路径。另含一个 PPO 强化学习
番外实验（LunarLander 登月）。

## 关键指标一览

| 阶段 | 模型/策略 | 指标 | 结果 |
|------|-----------|------|------|
| 1 | 矩阵分解（最优超参） | Recall@10 | 0.062 |
| 2 | LR / FM / DeepFM | 已评分样本正向偏好 AUC | 0.707 / 0.752 / **0.754**（时间切分；非真实 CTR）|
| 2 | DeepFM | **user-level GAUC** | **0.735**（91.6% 用户覆盖）|
| 3 | 双塔 + logit 校正 | Recall@10 | **0.098**（leave-one-out；vs MF 0.062，+58%）|
| 3 | 双塔（**全局时间切分**，无时间穿越） | Recall@10 | **0.069** [0.055, 0.085]，为 LOO 的 70.6% |
| 3+2 | **端到端 召回50→精排10** | Recall@10 | 原版 **0.065**（显著劣于不精排的 0.098）；六版重训后 v6 达 **0.094**，缺口补回 88.7%，CI 跨 0 = 与不精排持平 |
| 4 | LinUCB vs ε-greedy | 在线 CTR | **0.704** vs 0.671 |
| 番外 | PPO（LunarLander） | 平均奖励 | **255.6**（>200 学会线）|
| 序列推荐 | SASRec（d64/2层/L200） | 测试 HR@10 / NDCG@10 | 0.791 / 0.543（3 层消融 0.797 / 0.549）|
| 会话 RL | 手写 PPO vs 策略梯度（采样部署） | 仿真会话奖励 / 多样性 | **15.43** / 6.87 vs 15.86 / 5.42（argmax 均塌缩；贪心去重基线 17.93）|
| Agentic RL | 学出来的门控 vs 规则门（成本扫描） | 探索性相变候选 | 3 seed 均值在 cost≤0.3 规则更高、cost=0.8 时 RL 高 +1.92；正式推断待 ≥10 seed |
| 后训练 | 离线 RL（naive DQN）vs SFT | 仿真会话奖励 | **8~13.6** vs 2.87（SFT=行为策略天花板）|

> 注：双塔 Recall@10 从 0.097 修正为 **0.098** —— 原评估的检索缓冲区固定多取 50 个，
> 重度用户（最多 1434 个正样本）过滤已看后凑不满 10 条，3552 个用户里有 243 个被
> 截断，指标被自己低估。修复后与端到端脚本的"仅召回"对照数字完全一致，两条独立
> 路径互相验证。

## 系统架构（Demo 实现的工业流水线）

```
用户请求
   │
   ▼
┌─────────────────┐   用户塔出 32 维向量
│  双塔召回        │   Faiss IndexFlatIP 全库检索（3706→50 候选，毫秒级）
│  （默认直接 Top-K）│   ranking_policy=retrieval，不加载 DeepFM
└─────────────────┘
   │
   ▼
┌──────────────────────────────┐   仅显式实验：融合用户画像+电影特征+交叉信息
│  DeepFM / v6 残差精排（可选） │   已被端到端评估判为不晋升，只做失败复现
└──────────────────────────────┘
   │
   ▼
推荐结果（Streamlit 界面展示，可切换任意用户）
```

## 产出清单

- **图表**（`figures/`）：数据探索×3、矩阵分解过拟合对比、PPO 奖励曲线、
  PPO 着陆 GIF、CTR-style 三模型偏好分类对比、双塔召回对比、Bandit 探索-利用曲线
- **模型 checkpoint**（`checkpoints/`）：two_tower.pt（默认）、mf.pt、
  deepfm.pt（失败复现）、deepfm_rerank_v1~v6.pt、sasrec_main.pt、ppo_lander.zip 等
- **实验日志**（`experiments/`）：训练 CSV、端到端/协议/召回基准/OPE/Agentic 报告
- **Demo**：`app.py`（`.venv/Scripts/python.exe -m streamlit run app.py`）
- **学习笔记**（`notes/`）：20 篇（15~20 为端到端、召回基准、多路排序、OPE 与
  TIGER、多工具轨迹）

## 工程化落地 & 后训练（Phase 2b）

在研究流水线之上补齐三块工程能力（详见 `notes/11`、`notes/12`）：

- **可测试**：`tests/` 标准库 `unittest` 冒烟套件（CPU-only，`--test` 一键跑；
  用例数以实际输出为准，不在文档里硬编码），覆盖指标/序列模型/RL 环境/服务核心/
  离线 RL/多 worker 指标与生命周期/端到端评估/成本扫描聚合。
- **可部署**：`src/serving.py`（可复用 `Recommender`）+ `serve.py`（Starlette REST API，
  零新增依赖）；`app.py` 与 API 同源。API 具备 live/ready、跨 worker 指标、
  Prometheus、CPU/RSS/事件循环观测、速率/并发保护与优雅停机。当前默认双塔路径
HTTP 端到端 **5.42 ms / p95 9.67 ms**（2026-08-16 WSL/Linux CPU、SQLite@/tmp、
c1×200 实测），推荐核心 **0.19 ms**；旧的 Windows DeepFM 路径 4.6 ms 仅作历史参考。
- **后训练**：`research_v2/phase2_offline_rl/`——推荐版 SFT→RL。**诚实结论**：SFT（行为克隆）
  忠实复刻行为策略、无法超越其天花板（2.87）；离线 RL（Bellman）越过它、学出交错策略
  （8~13.6）；CQL 的保守在本仿真器（回报有界 + 动作全合法）里反而有害——**先诊断数据覆盖，再决定是否上保守**。

## SASRec 消融实验矩阵（2026-08-04 完成）

补全课题设计书 W2 里程碑（`notes/08` 待办）。协议与主模型一致：ML-1M、
leave-one-out + 100 负采样、100 epochs、验证集 NDCG 选优后测测试集；
基线 = `sasrec_main.pt`（d64 / 2 层 / maxlen 200 / dropout 0.2），每个变体只改一个轴。

| 变体 | 最佳验证 NDCG@10 | 测试 HR@10 | 测试 NDCG@10 |
|------|-----------------|-----------|-------------|
| 基线 | 0.5673（ep 88） | 0.7909 | 0.5428 |
| d=128 | 0.5420（ep 70） | 0.7788 | 0.5156 |
| **blocks=3** | **0.5750**（ep 100） | **0.7972** | **0.5489** |
| maxlen=50 | 0.5377（ep 40） | 0.7659 | 0.5107 |
| dropout=0.5 | 0.5418（ep 100） | 0.7791 | 0.5174 |

**结论**：加深（3 层）有真实正收益；加大宽度、缩短序列、提高 dropout 均变差。
ML-1M 数据量小（3,706 物品），d=64 已足够；长历史（maxlen 200）是序列推荐的核心价值。
产物与复现脚本：`experiments/sasrec_ablation_matrix.csv`、
`scripts/sasrec_ablation_summary.py`、`figures/sasrec_ablation_*-20260804.png`。

## 端到端联合评估：分段指标好看，整条链路却是负增量（2026-08-15）

此前项目只分开报两个数字——双塔 Recall@10 = 0.098、DeepFM AUC = 0.754——但旧版线上
没有任何请求只跑其中一段。补上 `scripts/eval_end_to_end.py` 后，第一次量出了真实
链路（双塔召回 50 → DeepFM 精排 → Top-10）的指标，结论是**负的**：

| 环节 | Recall@10 | NDCG@10 | 说明 |
|---|---|---|---|
| 召回天花板 Recall@50 | **0.307** | — | 真实物品进候选池的比例，整条链路的上限 |
| 仅召回（不精排，取双塔前 10） | **0.098** | 0.046 | 对照组 |
| **端到端（召回→精排→Top-10）** | **0.065** | 0.031 | 线上真正在跑的 |
| 精排增量 | **−0.033（−33.6%）** | −0.016 | `ranking_hurts` |

精排把真实物品**前移 451 次、后移 620 次**；召回命中的物品里只有 21.2% 被精排留在
Top-10。协议：leave-one-out，3552 个用户，走 `Recommender.recommend` 与线上同一条
代码路径。

**根因（曝光集中度诊断）**：

| 顺序来源 | Top-10 去重物品数 | 前 20 个物品占全部曝光 |
|---|---|---|
| 双塔召回 | **1853** | 17.0% |
| DeepFM 精排后 | **773** | **40.0%** |

精排把候选集**坍缩到了热门头部**：《辛德勒的名单》出现在 35% 用户的推荐里，头部
八部全是 ML-1M 里评分数 2200–3000 的公认经典。也就是说 DeepFM 的 pointwise
正向评分目标主要学到了**物品质量先验**，而不是"这个用户会不会喜欢它"。

这与同期补的 GAUC 指标互相印证：全局 AUC 0.7535，**user-level GAUC 仅 0.7347**
（1108 个用户参与，91.6% 覆盖）。全局 AUC 里有一部分区分度来自"哪些电影整体被打
高分"，GAUC 只认同一用户内部的排序，更接近线上体验。

**这是典型的 sample selection bias**：DeepFM 训练时只看到用户主动评分过的
(user, item) 对，区分"好电影 vs 烂电影"就能拿到 0.75 的 AUC；服务时却要给
双塔选出的 50 个高相关候选打分，其中很多组合从未进入评分样本分布。此时候选
普遍质量较高，需要的是**用户级相关性**——这个分布它没训过。

**待办（按优先级）**：
1. ~~用双塔召回的候选做负采样重训 DeepFM~~ → **已完成，见下一节 v1**
2. ~~把 pointwise BCE 换成 pairwise/listwise~~ → **已完成，见 v2**
3. ~~加用户-物品交叉特征~~ → **已完成，见 v3**
4. ~~在这些做完之前，线上应当直接使用双塔 Top-10~~ → **结论依然成立**：
   六版迭代（含 out-of-fold 交叉拟合）后最好的 v6 也只做到"与不精排统计上
   分不出"，没有一版打败基线。

复现：`.venv/Scripts/python.exe scripts/eval_end_to_end.py --tag <new-tag>`；
产物 `experiments/eval_end_to_end_e2e-v1.json`。

## 修复精排负增量：六版迭代与最终决策（2026-08-15）

按上一节的待办逐条实施，每版**只改一件事**，全部走同一条评估路径
（`Recommender.recommend`，召回与协议一字不变，差异只来自 ranker 权重）。
新增 `src/train_reranker.py`。判定用配对 bootstrap 95% 区间——单位是用户，
两条路径在同一批用户上观测，与 `src/feedback.py` 的 OPE 决策门同一个估计器。

| 版本 | 改了什么 | Recall@10 | NDCG@10 | Δ vs 仅召回 | 95% CI | 判定 |
|---|---|---:|---:|---:|---|---|
| v0 | 原版（全库随机负样本） | 0.0650 | 0.0305 | −0.0329 | [−0.0445, −0.0220] | **显著更差** |
| v1 | 负样本换成双塔召回候选 | 0.0769 | 0.0377 | −0.0211 | [−0.0327, −0.0093] | 显著更差 |
| v2 | + listwise softmax | 0.0808 | 0.0371 | −0.0172 | [−0.0290, −0.0062] | 显著更差 |
| v3 | + 用户-物品交叉特征 | 0.0870 | 0.0417 | −0.0110 | [−0.0197, −0.0023] | 显著更差 |
| v4 | 负样本截到 serve_k=50 | 0.0774 | 0.0332 | −0.0206 | [−0.0307, −0.0104] | 显著更差（**变差了**）|
| **v5** | **残差：从复刻召回起步** | **0.0923** | **0.0430** | **−0.0056** | **[−0.0135, +0.0020]** | **统计上分不出** |
| **v6** | **v5 + 4 折交叉拟合特征** | **0.0943** | **0.0455** | **−0.0037** | **[−0.0132, +0.0053]** | **统计上分不出（最优）** |
| — | 仅召回（双塔 Top-10） | 0.0980 | 0.0463 | — | — | 要打败的基线 |
| — | 召回天花板 Recall@50 | 0.3074 | — | — | — | 整条链路上限 |

**曝光集���度（Top-10 去重物品数 / 前 20 占比，双塔本身 1853 / 17.0%）**：
v0 的 773 / 40.0% → v1 的 2308 / 22.1% → v5 的 2011 / 14.3% → v6 的 1437 / 18.7%。
**热门坍缩在 v1 就被彻底修好了**——只换负样本分布，去重物品数直接翻三倍，
这证实了 sample selection bias 的诊断是对的。v6 最接近召回本身的分布形态，
真实物品被前移 520 次、后移 517 次（近乎持平），与"基本照抄召回"的门控诊断一致。

**每一版的教训**：

1. **v1（+0.0119）**：负样本从全库随机换成召回候选，是收益最大的单项改动。
   印证根因诊断——原版从没在"候选全是好电影"这个分布上训练过。
2. **v2（+0.0039）**：pointwise BCE → 候选集内 softmax。目标从"预测绝对喜欢
   概率"变成"在候选集内排序"，与线上被问的问题一致。
3. **v3（+0.0062）**：把双塔相似度和用户类型偏好作为显式稠密特征喂进去。
   验证集 Hit@10 从 0.39 跳到 0.71 —— 原版模型里**没有任何一维直接刻画
   "这个用户和这个物品有多配"**，交叉全靠 FM/MLP 自己学，小数据上学不出来。
4. **v4（−0.0096，负面结果）**：把负样本进一步截断到线上真正面对的 top-50。
   假设是"训练该在真实决策边界上"，**结果被实验否定**。原因是隐式反馈里
   top-50 含大量**假负样本**（用户会喜欢但没评过分的电影），只用最难的负样本
   等于教模型压制真正的好物品。v3 那种"混合难度"才是对的——这与工业界
   hard negative mining 的经验一致：难负样本要掺，不能全用。
5. **v5（+0.0053，最优）**：改成显式残差
   `score = w·tt_score + gate·deepfm_logit`，`gate` 初始化为 0。
   **训练开始时输出与召回顺序完全一致**，精排只能在此之上做增量。
   v1~v4 全都从与召回无关的随机初始化出发，得自己重新学出召回已经编码好的
   相关性——这是它们始终追不平基线的结构性原因。
6. **v6（+0.0020，全局最优）**：4 折**交叉拟合**特征。线上那个双塔是用全部
   训练交互训好的，它对训练正样本的打分**乐观偏置**（当初就是被优化成
   "让 u·i 尽量高"的）；精排学到的 `tt_score` 权重是在这个乐观分布上拟合的，
   到测试集（双塔没训过的物品）就偏了。修法是把训练交互切成 4 折，
   第 k 个双塔只用其它 3 折训练，**只给第 k 折的样本算特征**——每个训练样本的
   `tt_score` 都来自一个没见过它的模型，与线上"目标物品是未见过的"完全同构。
   验证指标同时变诚实了（去掉乐观偏置后，同配置下验证 Hit@10 明显下降）。

**决定性诊断：残差门到底学到了什么？**

| 版本 | `w_retrieval` | `gate` | \|gate\|/w |
|---|---:|---:|---:|
| v5 | 19.86 | **−1.13** | 5.7% |
| v6 | 19.65 | **−1.22** | 6.2% |

两点值得注意：

- **门控权重只有召回权重的 6%** —— 模型自己学出来的结论是"基本照抄召回"。
- **门控是负的** —— 学到的 DeepFM 分量被当作**惩罚项**而非加分项。这与全部
  诊断自洽：DeepFM 编码的主要是物品热度，而在**已经按相关性召回过**的候选
  之间，额外的热度是负信号（热度已经被召回吃掉了，剩下的热度意味着个人相关性更弱）。

即便权重只有 6%，50 个候选里也只有 1 个位置与召回顺序相同——**小权重照样
大幅重排，而这个重排在测试集上净损失。**

**最终决策：不上线任何版本做全量替换，但 v0 必须下线。**

- v0~v4 的置信区间**完全落在 0 以下**，是被证实的净损失；
- v5 / v6 的区间**跨 0**，即"不比不精排差，也没有证据说更好"；
- 按本项目一贯的证据门槛（区间不含 0 才推全量），**没有一版够格晋升**。
- 线上正确做法：**直接使用双塔 Top-10**；若因业务规则必须保留精排层
  （多样性、去重、运营干预），则用 v6，它是缺口最小且不会主动伤害指标的版本。

该决策已经落实到代码：`Recommender.load()`、REST API、主 Demo 和反馈采集器均
默认使用 `ranking_policy=retrieval`，不会加载 DeepFM；只有显式选择
`ranking_policy=deepfm` 才进入失败复现/候选晋升评估路径。

**六版之后的结论（这才是真正的发现）**：缺口从 −0.0329 收窄到 −0.0037
（**补回 88.7%**），但**始终没有翻正**。每修掉一个偏差，模型就更接近
"精确复刻召回顺序"一点，却从未超越它。结合上面的门控诊断，最合理的解释是：

> **在 ML-1M 上、用这套特征，精排层没有召回层不知道的信息。**

这不是实现问题，是**信息问题**——要让精排真正有价值，它必须拿到召回看不到的
东西（实时上下文、序列行为、多目标信号、跨域特征），而不是把同样的信息
再学一遍。这也解释了为什么工业界精排的价值主要来自**特征丰富度**而非模型复杂度。

复现：
```powershell
.venv/Scripts/python.exe src/train_reranker.py --tag v6 --loss listwise --residual `
    --epochs 20 --oof-folds 4 --oof-epochs 20
.venv/Scripts/python.exe scripts/eval_end_to_end.py --tag <new> --deepfm-ckpt checkpoints/deepfm_rerank_v6.pt
```

## 评估协议对照：leave-one-out 把 Recall 高估了多少（2026-08-15）

项目此前所有召回指标都来自 **leave-one-out**（每个用户按时间的最后一条做测试）。
这个协议有一个已知缺陷：**时间穿越**——用户 A 的测试时刻可能早于用户 B 的某些
训练交互，模型等于用"未来的全局信息"预测"过去"。学术界 2020 年后对 leave-one-out
的主要批评就是这条。本项目此前只在文字上提醒过，这次把它变成了数字。

新增 `--split time`（全局时间切分：所有交互按时间排序，前 90% 训练、后 10% 测试）
和 `scripts/eval_split_protocols.py`：

| 协议 | 训练对 | 测试用户 | Recall@10 | 95% CI |
|---|---:|---:|---:|---|
| leave-one-out（既有口径） | 571,729 | 3,552 | **0.0980** | [0.0884, 0.1075] |
| 全局时间切分（无穿越） | 520,829 | 1,157 | **0.0691** | [0.0553, 0.0847] |

**区间不重叠** → 差距大于采样噪声。时间切分只有 leave-one-out 的 **70.6%**，
相对下降 **29.4%**。另有 31 个用户只出现在测试期（冷启动，embedding 从未训练），
已剔除并计数。

**必须一起说的三条边界**（都写进了脚本 docstring 和报告 JSON）：

1. 这是**两个模型 + 两个协议**的对照，不是同一个模型换协议评估——leave-one-out
   训出来的模型见过测试期的其它交互，直接拿去评时间切分测试集本身就是泄漏。
2. 时间切分的训练数据更少（520,829 vs 571,729 对），任务也更难（预测更远的未来），
   **所以 29.4% 里既有"消除穿越"的部分，也有"数据更少/任务更难"的部分，本实验
   无法拆开**。不能说"leave-one-out 恰好高估了 29.4%"。
3. 时间切分的测试用户只有 1,157 个，方差明显更大，所以必须报区间。

**为什么仍然保留 leave-one-out 作为主口径**：与 SASRec 复现、与项目既有全部结果
可比。正确做法是**两个都报、并标注差异**，而不是挑好看的那个——这正是本项目
`AGENTS.md` 里"不得跨协议横向比较指标"那条规则的具体执行。

复现：
```powershell
.venv/Scripts/python.exe src/train_two_tower.py --split time --epochs 20 `
    --ckpt checkpoints/two_tower_timesplit.pt
.venv/Scripts/python.exe scripts/eval_split_protocols.py --tag <new-tag>
```
产物 `experiments/eval_split_protocols_protocol-v1.json`；
不变量由 `tests/test_split_protocols.py` 锁死（核心一条：**时间切分下训练集里
不得存在任何晚于测试集的交互**，并用同一份数据证明 leave-one-out 会违反它）。

## Agentic 工具门控：成本相变扫描（2026-08-15）

此前"规则门 > GRPO，RL 没用"的结论只在**单个成本点**（tool_cost=0.03）上成立。
`scripts/agentic_cost_sweep.py` 沿成本轴扫了 6 个点 × 2 算法 × 3 seed = 36 组，
把单点结论升级成一条相变曲线（净奖励，GRPO 组 3 seed 均值）：

| tool_cost | always_baseline | always_tool | 规则门 | 学出来的(采样) | 最优 |
|---:|---:|---:|---:|---:|---|
| 0 | 2.775 | **17.958** | 17.958 | 15.121 | 无脑调工具 |
| 0.03 | 2.775 | 17.358 | **17.436** | 14.746 | 规则门 ← 原来唯一的实验点 |
| 0.15 | 2.775 | 14.958 | **15.346** | 13.259 | 规则门 |
| 0.30 | 2.775 | 11.958 | **12.733** | 11.439 | 规则门 |
| 0.50 | 2.775 | 7.958 | **9.250** | 9.085 | 规则门（但已分不出） |
| 0.80 | 2.775 | 1.958 | 4.025 | **5.940** | **学出来的策略** |

下表保留旧脚本对 3 个 seed 做出的 bootstrap 描述区间（学出来的采样策略 − 规则门）。
**seed 才是独立重复单位，n=3 不足以支撑正式置信区间或“显著”结论**；这些数只用于
观察方向，新脚本在少于 10 seed 时会直接隐藏区间并返回
`insufficient_seed_replication`。

| tool_cost | GRPO Δ净奖励 | 旧 3-seed 描述区间 | 探索性方向 |
|---:|---:|---|---|
| 0 | −2.837 | [−5.253, −1.497] | 规则门均值更高 |
| 0.03 | −2.690 | [−4.985, −1.407] | 规则门均值更高 |
| 0.15 | −2.087 | [−3.922, −1.046] | 规则门均值更高 |
| 0.30 | −1.294 | [−2.581, −0.582] | 规则门均值更高 |
| **0.50** | −0.165 | **[−0.802, +0.203]** | **方向不稳定** |
| **0.80** | **+1.915** | **[+1.742, +2.188]** | **学习策略均值更高；待复验** |

**探索性结论**：在这 3 个 seed 上，差距随成本**单调收窄**，并在 cost ≈ 0.5
附近出现穿越零点的候选；cost = 0.8 时 RL 均值更高，但是否稳定胜出必须用至少
10 个独立 seed 复验。原来的负面结论适用范围可能被高估——**在 tool_cost=0.03
这个点上，工具几乎总是划算的，最优解退化成"总是调用"，"学会何时调工具"这个问题
本身就不成立。** RL 的价值只在决策边界真正复杂、固定阈值刻画不了的时候才出现。

**GRPO vs REINFORCE**：GRPO 在全部 6 个成本点上都不低于 REINFORCE
（+0.798 / +0.748 / +0.545 / +0.325 / +0.137 / +0.094），但增量随成本递减。
注意 K=1 时 GRPO 的梯度与 REINFORCE **逐位相同**（ratio ≡ 1，裁剪永不触发），
因此扫描中 GRPO 一律用 K=4；细节见 `notes/14`。

复现：`.venv/Scripts/python.exe scripts/agentic_cost_sweep.py --tag <new-tag>`
（36 组约 33 分钟）；产物 `experiments/agentic_cost_sweep_cost-phase-v1.json`。

**边界**：仿真器证据。用户模型是冻结 SASRec + 手写疲劳/流失，成本轴本身也是人为
设定的；本实验量化的是"在这个仿真器里 RL 相对规则的增量如何随成本变化"，不是
"线上工具调用该收多少钱"。

## Agentic 多工具预算轨迹（2026-08-15）

二元“调用/不调用”门已扩成真正的决策轨迹。每次推荐可依次调用 `fatigue_scan`
（0.05）、`diversity_search`（0.03）、`preference_probe`（0.12），或随时
`STOP_AND_SERVE`；单次最多两工具、会话总预算 0.60。重复调用、超预算和非法动作
都有显式 mask/惩罚/计数，非法动作后强制停止，避免无限循环。用户会话仍由冻结
SASRec、疲劳/流失规则驱动，另加一个只有 preference tool 可揭示的隐藏类型偏好。

正式实验为 10 个独立训练 seed，每 seed 用 masked trajectory-level GRPO 训练，
并在 120 个同 seed 会话上统一比较：

| 策略 | 用户奖励 | 净奖励 | 工具/推荐 | 多工具决策率 | 决策 |
|---|---:|---:|---:|---:|---|
| one-step oracle（不可部署） | 16.820 | **16.275** | 0.755 | 0% | 信息上界 |
| **预算规则门** | 15.861 | **15.371** | 0.608 | 0% | 保留 |
| learned GRPO（采样） | 11.290 | 10.725 | 0.599 | 17.5% | 不晋升 |
| random unmasked | 9.801 | 7.259 | 0.586 | 16.8% | 非法动作率 60.6% |
| learned GRPO（argmax） | 2.745 | 2.745 | 0.001 | 0% | 退化为不调工具 |
| 直接推荐 | 2.737 | 2.737 | 0 | 0% | 基线 |

学习采样策略相对预算规则门的同 seed 配对净奖励差为 **-4.646**，95% bootstrap
区间 **[-4.974, -4.322]**，达到 10-seed 正式门槛并判定
`budgeted_rule_wins`。学习策略确实形成了多工具链且无非法调用，但几乎花完预算
（平均 0.566），还把会话长度从规则门的 19.98 降到 17.42；argmax 则完全坍缩到
停止。诊断是整会话单一 advantage 的信用分配过粗，且没有 SFT/规则轨迹 warm start。

因此多工具系统实现完成，但学习策略不晋升。下一轮研究应做 decision-level credit、
规则轨迹 SFT 后再 RL、以及工具结果移除/打乱消融，不能仅增加 rollout 数量。
该实验加入了新的隐藏偏好与独立工具成本，**不得与旧二元 tool_cost 扫描数值混算**。
正式产物：`experiments/agentic_multitool_p2-multitool-10seed-v1.json`；详见
`notes/20-Agentic多工具预算轨迹.md`。

## 滚动时间 + 全库召回基准（2026-08-15）

为避免只在一个 cutoff 上比较，新增 `scripts/benchmark_retrieval.py`：全局时间
70%/80%/90% 三个 expanding window，每窗预测后续 5% 时间段内每位 warm 用户的
第一条正向评分，候选是全部未看电影（无采样负例）。双塔每窗训练 3 个 seed、
10 epochs，并和 MostPopular / Item-kNN 同协议对照。

| 模型 | W1 | W2 | W3 | Recall@10 三窗宏平均 | 最差窗口 |
|---|---:|---:|---:|---:|---:|
| MostPopular | **0.0963** | **0.0675** | 0.0516 | **0.0718** | 0.0516 |
| Item-kNN | 0.0917 | 0.0516 | 0.0640 | 0.0691 | 0.0516 |
| TwoTower（3-seed 均值） | 0.0734 | 0.0675 | **0.0658** | 0.0689 | **0.0658** |

相关性宏平均没有证明神经模型更好，但双塔 catalog coverage@10 达 17%–30%，
MostPopular 仅 1.5%–2.6%；双塔 novelty 约 9.4–9.6 bits，Popular 约 8.0–8.1，
且跨窗口 Recall 标准差更低（0.0032 vs 0.0185）。所以当前更诚实的判断是：
**双塔的价值主要体现在覆盖、新颖度和最差窗口稳定性，不是 headline Recall 胜出。**
权威产物为 `retrieval_benchmark_p1-rolling-fullcatalog-3seed-v2`；v1 的 Item-kNN
过滤历史有 bug，只保留作审计，不得引用。详见 `notes/16`。

## 多路召回 + 序列感知残差 listwise（2026-08-15）

在同一滚动协议上，将 TwoTower / Item-kNN / Popular 的 Top-200 用 RRF 融合。
三窗 Recall@10（3-seed 均值）为 0.0994 / 0.0622 / 0.0744，宏平均 **0.0787**，
探索性高于最强单路 Popular 0.0718；但 coverage@10 只有 3.5%–5.0%，远低于
双塔的 17%–30%。这是一条**相关性—覆盖率交换**，不足以替换默认策略。

随后实现有界残差的序列 listwise ranker，使用路线排名、近期类型偏好、最后物品
类型重合、热门度与长尾特征。为避免泄漏，窗口 t 只准使用窗口 `<t` 当时诚实生成
的候选/标签训练，W1 因没有历史窗口而跳过。W2/W3 六个 seed-window 配对：

| 策略 | Recall@10 均值 |
|---|---:|
| RRF | 0.068309 |
| 序列残差 listwise | 0.068783 |
| 配对增量 | **+0.000474**（范围 −0.003367 ~ +0.003968）|

结论是**无稳定增益，不晋升**。Candidate Recall@200 仅 44%–51%，表明优先级应是
增加互补召回路线，而不是继续堆 ranker。代码/证据见
`src/multistage_retrieval.py`、`scripts/eval_multiroute_ranker.py`、`notes/17`。

## TIGER-lite Semantic ID 生成式召回（2026-08-15）

按 TIGER 的核心机制实现受控基线：标题/类型/年代内容向量先做三级残差 K-Means，
得到 `64×64×64` 语义 token，再追加 collision ordinal 保证 3,706 个物品 ID 一一
对应；Transformer encoder-decoder 根据历史语义 ID 自回归预测下一物品 ID。评估
对所有未看物品的唯一 ID 计算精确序列 log-probability，不使用采样负例或 beam 近似。

这不是原论文的精确复现：MovieLens-1M 缺少丰富商品文本，因此使用仓库内可复现的
hashed title/genre/decade + RQ-KMeans，替代 SentenceT5 + learned RQ-VAE。正式协议
仍与 P1 完全一致，并在每个外层训练窗内部用最后 10% 时间片选择 epoch，再从同 seed
在完整外层训练窗重训，外层未来只评估一次。

| 模型 | W1 | W2 | W3 | Recall@10 宏平均 | 平均 coverage@10 |
|---|---:|---:|---:|---:|---:|
| TIGER-lite（3-seed 均值） | 0.0749 | 0.0463 | 0.0460 | **0.0557** | 2.75% |
| Popular | 0.0963 | 0.0675 | 0.0516 | 0.0718 | 2.13% |
| Item-kNN | 0.0917 | 0.0516 | 0.0640 | 0.0691 | 5.68% |
| TwoTower | 0.0734 | 0.0675 | 0.0658 | 0.0689 | 21.79% |

生成模型没有赢，也没有展现论文强调的冷物品能力：W3 仅有 4 个冷目标，三个 seed
均为 0 命中，样本太小不能作一般性冷启动结论。主要诊断是内容语义与交互目标对齐
不足、长尾份额近零、生成路径仍偏头部。结果不晋升；下一步若继续，应换含丰富内容与
足够冷物品的 Amazon/Steam 协议，或学习 RQ-VAE，而不是在 ML-1M 上继续堆 epoch。
证据：`experiments/generative_retrieval_p2-tiger-lite-rolling-3seed-v1.json`；
实现与边界见 `notes/19-TIGER语义ID生成式召回.md`。

## 最重要的五条经验

1. **损失在降 ≠ 模型在变好**：矩阵分解训练 loss 一路降，Recall 却崩了
   （过拟合）；L2 正则 + 验证集选优是基本功
2. **评估口径和采样偏差是隐形杀手**：双塔第一版比矩阵分解还差，
   对齐评估协议 + logit 校正后反超 58% —— 数字不好先查这两点
3. **特征质量 > 算法高级**：LinUCB 带人口属性特征输给 ε-greedy，
   换上有信息量的口味特征后才突破非个性化天花板
4. **分段指标全绿不代表链路是对的**：双塔 Recall 0.098、DeepFM AUC 0.754
   都很体面，端到端却只有 0.065——**比不要精排还差**。没有端到端评估，
   这个负增量可以在线上活很久而无人发现。任何多阶段系统，必须有一个
   指标是端到端的。修它花了六个版本（含 out-of-fold 交叉拟合），最好的结果也只是**打平**——
   **修一个架构性错误的成本，远高于一开始就把评估做对。**
5. **负面结论也有适用范围**：`规则门 > RL` 的单点观察只来自 tool_cost=0.03；
   扫完成本轴后，3-seed 均值在 cost=0.8 反向，但这仍只是待 ≥10 seed 复验的候选——
   **单点实验和少量随机种子的否定/肯定结论都不可靠。**

## 后续可扩展方向

- 真实反馈与 OPE V2 已完成：推荐/曝光/反馈可追踪；schema-v2 回放按
  `policy_name/model_version` 强制隔离，提供 item-level IPS/SNIPS、交叉拟合 DR、
  actor-cluster bootstrap、支持度与样本质量门。正式决策仍使用预注册 IPS，DR 只做
  稳健性复核。
- 真人采集与正式评估 V2 已落地：`feedback_app.py` 在显式有限探索下采集匿名
  真人偏好；候选“双塔相似度 + 类型多样性”通过配对 IPS、bootstrap 95% 置信区间、
  200 批推荐/5 位匿名体验者/95% 曝光覆盖/ESS 100 门槛后才允许形成策略决策。
- 合成校准 `p2-ope-calibration-5000-v2` 的 oracle 策略增量为 0.02684；IPS /
  SNIPS / DR 增量绝对误差为 0.00028 / 0.00859 / 0.00812，证明 DR 不会在结果模型
  失配时自动胜过正确 propensity 的 IPS。真实历史探索队列只有 21 次推荐、1 位
  体验者，IPS 与 DR 方向冲突，质量门给出 `collect_more_data`；不得据此宣称收益。
- 为降低单人采集负担，实验台默认增加 20 批快速个人试验：每批展示 5 部且只需
  点选最想看的一部，点击后自动进入下一批，第 20 批后自动停止。该模式独立记录
  `policy_name`，只输出 90% 区间下的个人方向性信号，不替代上述正式门槛。
- 外部真人问卷验证使用 GroupLens Personality 2018（1,834 人；预设四组对照
  728 人）：中等多样性的观看意愿原始均值略高但区间跨 0，高多样性则显著降低
  感知个性化 0.332/5 分（Holm 校正 `p=0.0144`）。结合本地个人试验，决策为
  “保留个人候选、不做全局上线”。详见 `notes/13-外部问卷验证多样性策略.md`。

- 序列推荐（GRU4Rec / SASRec）：把用户历史序列喂进 Transformer
- 多步 RL 推荐：把用户会话建模成 MDP，用 DQN/PPO 优化长期留存
- 线上评估：A/B 测试设计、离线评估与线上指标的一致性分析
