# 项目导学：从零看懂每一步

> 这份文档是给**零基础的你**准备的"入口地图"。
> 不需要任何前置知识，从头到尾用大白话讲清楚：
> 这个项目在做什么、为什么这么做、每一步学会了什么。
> 读完后，你再去看 `notes/` 里的 9 篇笔记和 `src/` 里的代码，就不会迷路了。

---

## 0. 30 秒版本

我们做了一件事：**从 scratch 搭建了一个类似"猜你喜欢"的电影推荐系统，
然后用强化学习教它"不要短视"**。

- 前半部分（Phase 1）：把互联网公司的推荐系统流水线完整走了一遍——
  召回、排序、在线探索，每个环节都做了模型并跑出了真实指标。
- 后半部分（Phase 2）：像研究生一样做了一个小课题——发现"只追求即时
  点击率"的推荐会把用户气跑，于是用强化学习（DQN 和策略梯度）证明：
  **考虑长期利益的推荐，收益是朴素贪心的 5.6 倍**（并诚实记录：贪心去重基线 17.93）。

全程用的都是真实数据（MovieLens 电影评分）和你自己的显卡。

---

## 1. 先建立直觉：推荐系统是什么？

打开抖音、淘宝、B站，首页看到的内容都不是随机摆的，而是系统从
**几百万个候选**里挑出**你最可能感兴趣的几十个**。这个过程分两步：

```
几百万条内容                                你看到的几十条
     │                                        ▲
     ▼                                        │
┌─────────┐   粗选出几百条   ┌─────────┐   精细打分排序
│  召回   │ ──────────────► │  排序   │ ──────────────►
│ (双塔)  │                  │(DeepFM) │
└─────────┘                  └─────────┘
```

- **召回**：快但粗。像图书管理员凭印象从整个图书馆里先抱来一摞书。
- **排序**：慢但准。仔细看这一摞书，按"你会点开的概率"排好队。

Phase 1 就是把这两个零件分别做出来；Phase 2 则追问了一个更深的问题：
**"排序排得准"就够了吗？**（答案：不够，往下看第 4 节。）

---

## 2. Phase 1 逐阶段讲解（工程篇）

每个阶段都对应一篇笔记（`notes/0X`）、一张图（`figures/`）和可运行的
代码（`src/`）。建议按顺序读。

### 阶段 0：数据探索 —— 先看看原材料长什么样
做饭前先认食材。用 pandas 把 100 万条电影评分翻了一遍：评分集中在
3~4 分、少数用户贡献了大量评分、喜剧和剧情片最多。
📄 `notes/01-数据探索.md`　🖼 `figures/01~03`

### 阶段 1：矩阵分解 —— 最经典的推荐算法
核心思想一句话：**把每个用户和每部电影都变成一串数字（叫 Embedding，
可以理解成"口味坐标"）**。喜欢相似电影的人，坐标自然靠得近。
我们是手写 SGD 训练出来的，没用任何现成推荐库——这样你才能真正理解
Embedding 到底是什么。
📄 `notes/02-矩阵分解.md`　🖼 `figures/04`

### 阶段 1.5：PPO 快速体验 —— 先尝一口强化学习
在推荐之外的场景（控制登月器着陆）体验了 RL 的标准流程：
**做动作 → 拿奖励 → 改策略 → 再试**，重复几千次就学会了平稳着陆。
这里用的 PPO 就是你听说过的那个算法（训练 ChatGPT 用的也是它的亲戚）。
📄 `notes/03-PPO快速体验.md`　🖼 `figures/05~06`

### 阶段 2：DeepFM —— 预测"被点开的概率"
互联网公司的核心指标之一是 CTR（点击率）。DeepFM 是一个深度学习模型，
输入"用户特征 + 电影特征"，输出"会被点开的概率"。我们的版本 AUC 达到
0.754（0.5 等于瞎猜，1.0 是神仙，0.8 是工业可用水平）。
📄 `notes/04-CTR预估.md`　🖼 `figures/07`

### 阶段 3：双塔召回 —— 把"坐标"思想工业化
用户一座塔、物品一座塔，各自算出自己的 Embedding，用向量检索工具
Faiss 毫秒级找出"距离最近的几百部电影"。比阶段 1 提升了 56%。
📄 `notes/05-双塔召回.md`　🖼 `figures/08`

### 阶段 4：Bandit 探索 —— 边服务边学习
前面的模型都是"离线训练"：用历史数据学好，再上线。但新电影没有历史
数据怎么办？Bandit（多臂老虎机）策略会说：**拿出一小部分流量去"试水"
（探索），大部分流量推当前最优（利用）**，边干边学。
📄 `notes/06-Bandit探索利用.md`　🖼 `figures/09`

### 阶段 5：Streamlit Demo —— 把模型变成看得见的产品
一个网页小应用：选一个用户，实时展示"双塔召回 → DeepFM 精排"的完整
过程。面试时这就是你的演示神器。
📄 `app.py`（运行方式见 README"运行 Demo"一节）

---

## 3. Phase 2 逐模块讲解（研究篇）

Phase 2 换了一种节奏：不再是"学零件"，而是像研究生一样做课题——
**复现论文 → 做消融 → 提出自己的问题 → 设计实验回答它**。

### 模块 1：复现 SASRec —— 会"读历史"的推荐模型

前面的模型看用户是静止的（一堆特征的集合）。SASRec 把用户的观影记录
当成**一段有顺序的故事**来读："他先看了《泰坦尼克》，又看了《诺丁山》，
那下一步大概率还想看爱情片"。它用的结构叫 Transformer（就是 ChatGPT
的那个 Transformer 的单向版本）。

我们的复现结果和论文报告的几乎一致，然后做了**消融实验**——这个词
听着唬人，其实就是"控制变量法"：每次只拆掉一个零件（层数、维度、
序列长度、dropout），看成绩掉多少，就知道这个零件有多重要。
📄 `notes/08-SASRec复现.md`

### 模块 2：本课题的核心 —— 用 RL 治"推荐短视病"

**问题**：排序模型追求的是"这一次推荐被点开的概率最大化"。但如果系统
每次都推"最可能被点的"，就会一直给用户塞同一类电影——用户会腻、会走。
短期点击率好看了，长期用户流失了。这就是"短视"。

**难点**：没法拿真人做实验。怎么办？

**办法**：造一个**虚拟用户**来陪练。我们用训练好的 SASRec 扮演用户的
"喜好大脑"，再给它加上两个人性化的脾气：
1. **会腻**：连续推同一类型，兴趣逐次打 75 折；
2. **会走**：连续 3 次推荐不感兴趣，直接关掉 App。

然后让几种策略在这个虚拟环境里比赛，各服务 300 个"用户"：

| 策略 | 它的行为方式 | 总收益 | 用户平均停留 |
|------|-------------|--------|-------------|
| 贪心 | 永远推"此刻最可能被点的" | 2.81 | 7.7 步就腻了 |
| 随机 | 闭眼乱推 | 6.69 | 13.2 步 |
| DQN（价值型 RL） | 学会"换着类型推" | 7.63 | 13.7 步 |
| **策略梯度（PPO 的祖先）** | 学会"有策略地换花样" | **15.86** | **19.5 步** |
| **贪心去重（不重复类型）** | 5 行规则，跳过同类型 | **17.93** | **20.0 步** |

> 注意：贪心去重这个"5 行规则"反而最高——本仿真器的疲劳奖励让
> "交错类型"近乎免费，RL 的优势主要来自对比了一个太弱的贪心基线。

贪心的多样性只有 1.00——它 20 次推荐全塞同一个类型，把自己玩死了。
这个结果就是整个课题想证明的事。

📄 `notes/09-手写DQN与策略梯度.md`　🖼 `figures/10~12`

### 模块 2 番外：一次"失败"比成功更值钱

第一次训练 DQN 时彻底失败了——模型内部一个叫 "Q 值"的预估数字爆炸到
几百（理论上不该超过 9），训练出来的策略比瞎推还差。

这就是强化学习里著名的**"致命三要素"**：当 ①用神经网络做函数逼近、
②用"猜的答案去更新猜的答案"（自举）、③用旧数据训练（离策略）
三件事同时发生时，误差会像滚雪球一样越滚越大。

我做了三处修复（换成硬目标同步、加 LayerNorm、降低学习率和更新频率），
Q 值立刻乖了，稳定收敛到合理范围。
**面试时如果被问"你调试过最难的问题是什么"，这就是标准答案级别的素材。**

### 模块 3：证明它能长大 —— 2500 万条数据

把数据规模从 100 万条升级到 2500 万条（ML-25M），验证训练管线不会垮：
物品数量是原来的 16 倍，3 个 epoch 就收敛。说明这套东西不是玩具，
真的能scale。

---

## 4. RL 知识线：从 Bandit 到 GRPO 的一张大图

你说你了解 PPO / GRPO，这张图把本项目涉及的方法和它们串成一条线：

```
多臂老虎机 Bandit          只看一步：哪个选择期望收益最高
   │ + 考虑"当前状态"（用户是谁）
上下文 Bandit（阶段4）      还是只看一步，但会"看人下菜"
   │ + 考虑"未来"（用户会腻、会走）
完整强化学习 MDP（模块2）   目标变成：长期总收益最大
   │
   ├── 价值派：DQN         学"每个动作值多少分"，选分最高的
   │
   └── 策略派：REINFORCE    直接学"每个动作被选的概率"
        │ + 加个"评论员"压方差
        Actor-Critic（本项目用的）  演员演戏，评论员打分
        │ + 限制每次更新幅度，防止一步改崩
        PPO（你了解的）       工业界最常用
        │ + 去掉评论员，用"组内平均分"当基准
        GRPO（你了解的）      训练大语言模型常用（如 DeepSeek）
```

所以你在项目里手写的 Actor-Critic，就是 PPO/GRPO 的直系祖先——
理解了它，再看 PPO 的论文会顺很多。

---

## 5. 自己动手清单

所有训练命令都先 `cd "D:\machine  learning\recsys-learning"`，
用项目虚拟环境运行（`.venv/Scripts/python.exe`），画图用 `python`：

| 顺序 | 命令 | 耗时 | 你会看到 |
|------|------|------|----------|
| 1 | `.venv/Scripts/python.exe src/train_pg_rec.py --updates 50` | ~1分钟 | 策略梯度奖励从 4 分往上涨 |
| 2 | `.venv/Scripts/python.exe src/train_pg_rec.py --updates 0 --resume` | ~3分钟 | 四种策略的 300 会话公平对决 |
| 3 | `streamlit run app.py` | 一直开着 | 网页版推荐 Demo |
| 4 | `.venv/Scripts/python.exe src/train_dqn_rec.py --steps 3000 --tag my` | ~3分钟 | 手写 DQN 的学习过程 |

读代码的推荐顺序：`src/train_pg_rec.py`（约 150 行，注释最多）→
`src/train_dqn_rec.py`（环境设计部分）→ `src/train_sasrec.py`。

---

## 6. 术语速查表

| 术语 | 一句话解释 |
|------|-----------|
| Embedding | 把用户/电影变成一串数字坐标，爱好相近坐标就近 |
| CTR / AUC | 点击率 / "打分准不准"的指标，0.5=瞎猜，0.8=可用 |
| Recall@10 | 召回的 10 个结果里，命中用户真实喜欢了吗 |
| HR@10 / NDCG@10 | 前 10 名命中了吗 / 命中的位置靠前吗 |
| 消融实验 | 控制变量：每次只改一个零件，看它重不重要 |
| 状态 / 动作 / 奖励 | RL 三要素：处境 / 能做的事 / 做完得的分数 |
| Q 值 | "从这个状态做这个动作，未来大概能得多少分"的预估 |
| 自举 (bootstrap) | 用"猜的答案"去更新"猜的答案"（DQN 的核心，也是它发散的根源） |
| 策略熵 | 策略的"随机程度"。熵高=花样多，熵低=认死理 |
| 过估计 | max 操作偏爱"运气好被高估的"动作，误差滚雪球 |
| argmax 部署 | 永远选概率最大的动作（确定性） |
| 采样部署 | 按概率抽签（保留多样性）——本课题证明它强得多 |
| checkpoint / resume | 训练存档 / 从存档接着练 |

---

## 7. 常见困惑 FAQ

**Q：为什么随机推荐反而比贪心好？**
A：贪心每次都推"最可能中的"，结果 20 次全塞同一类型，用户被腻走；
随机虽然单个不准，但花样多不重复，用户反而待得久。这正说明"多样性"
在长期指标里有多值钱。

**Q：策略梯度 15.86 分是不是作弊？**
A：不是。它和所有策略面对完全相同的用户流（相同随机种子）、相同的
环境规则，赢得堂堂正正。它学会的是"交错推荐高价值类型"这个真正的
最优策略。

**Q：为什么 argmax 部署效果掉那么多（15.86→5.07）？**
A：训练出来的是一个"概率分布"——比如"40% 推喜剧、35% 推剧情、25% 推
动作"。argmax 把它压成"永远推喜剧"，退化成了贪心。这个环境的最优策略
**本质上就是随机的**，就像石头剪刀布的最优策略是各出 1/3。

**Q：虚拟用户会不会太假？**
A：这是学术界标准做法（叫 simulation-based evaluation），因为真实
线上实验需要几百万真实用户。它的局限我们也写进了研究报告第"结论与
局限"一节——知道边界在哪，本身就是科研训练的一部分。

**Q：我电脑跑得动吗？**
A：已经跑完了——所有实验都是在你这台机器上完成的，最快的实验
只要 1~3 分钟。

---

> 下一步建议：读完这份导学 → 读 `notes/07-RL求职知识手册.md` →
> 按第 5 节亲手跑一遍 → 再回头读 `notes/08/09` 两篇研究笔记。
> 遇到任何不懂的段落，直接问。
