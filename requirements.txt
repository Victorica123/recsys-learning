# 核心数据分析
pandas==3.0.3
numpy==2.5.1
matplotlib==3.11.1
seaborn==0.13.2

# 向量检索
faiss-cpu==1.14.3

# Demo / 服务化（Starlette/Uvicorn 随 streamlit 生态安装）
streamlit==1.59.2
starlette==1.3.1
uvicorn==0.51.0

# 深度学习。若先按 README 安装了 torch==2.9.1+cu128，本行会视为已满足；
# 否则安装默认发行版，足以运行 CPU 测试与训练。
torch==2.9.1

# 通用 RL 环境（LunarLander/Stable-Baselines3 放在 pyproject 的 lander extra）
gymnasium==1.2.2

# 开发工具（可选）：pip install -e ".[dev]" 或单独安装
# ruff>=0.6
# coverage>=7.0
